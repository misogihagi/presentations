{
  "name": "consultant",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p dist && npx marp --html slide.md > dist/index.html",
    "pptx": "@marp-team/marp-cli@latest --pptx-editable  --theme gradient.css slide.md",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@marp-team/marp-cli": "^4.1.2",
    "@marp-team/marp-core": "^4.0.1"
  }
}
